query GetBacklogItems($projectName: String!) {
  repository(owner: "owner", name: $projectName) {
    issues(states: OPEN, first: 100) {
      nodes {
        title
        body
        url
        labels(first: 5) {
          nodes {
            name
          }
        }
      }
    }
  }
}
